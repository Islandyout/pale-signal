# Pale Signal Prototype

Place the current canonical playable prototype in this folder as:

`prototype/pale-signal.html`

The Project Lead and automated studio build cycle will treat that file as the current browser prototype baseline until the production-engine migration is approved.

Do not replace it with older builds. When updating the prototype, preserve a working baseline and note the change in the Project Lead issue.
